ROOT_PATH=`realpath $(dirname $0)/../`
HOME_PATH=`realpath ~/`

DOCKER_PATH="$ROOT_PATH/docker"
MYSQL_PATH="$ROOT_PATH/mysql"
PHP_PATH="$ROOT_PATH/php"
HTML_PATH="$PHP_PATH/html"

LOGS_PATH="$ROOT_PATH/logs"
LOGS_APACHE_PATH="$LOGS_PATH/apache"
LOGS_MYSQL_PATH="$LOGS_PATH/mysql"
LOGS_PHP_PATH="$LOGS_PATH/php"

# DOCKER_IMAGE_BASE_NAME: Alpha-numeric, hyphen, underscore, and forward-slash only
DOCKER_IMAGE_BASE_NAME="loy-ev"

DOCKER_IMAGE_BASE="$DOCKER_IMAGE_BASE_NAME/base"
DOCKER_IMAGE_BASE_PATH="$DOCKER_PATH/images/base"

DOCKER_IMAGE_APACHE="$DOCKER_IMAGE_BASE_NAME/apache"
DOCKER_IMAGE_APACHE_PATH="$DOCKER_PATH/images/apache"

DOCKER_IMAGE_MYSQL="$DOCKER_IMAGE_BASE_NAME/mysql"
DOCKER_IMAGE_MYSQL_PATH="$DOCKER_PATH/images/mysql"

DOCKER_IMAGE_PHP="$DOCKER_IMAGE_BASE_NAME/php"
DOCKER_IMAGE_PHP_PATH="$DOCKER_PATH/images/php"

DOCKER_IMAGE_PHP_CLI="$DOCKER_IMAGE_BASE_NAME/php"

DOCKER_PORT_APACHE=9004
DOCKER_PORT_MYSQL=9001
DOCKER_PORT_PHP=9002

NVM_VERSION="v0.40.3"
NODE_VERSION="v22.16.0" # Currently just copies a built version to avoid potentially very long build time
